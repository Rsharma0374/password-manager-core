package com.password.manager.config;

import lombok.Data;

@Data
public class TokenValidationResponse {
    private boolean valid;
    // Add other fields as needed based on your validation API response
}
